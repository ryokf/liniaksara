import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['picsum.photos',"lh3.googleusercontent.com", 'liniaksara.com', 'liniaksara.id', 'liniaksara.org', 'images.unsplash.com'],
  },
};

export default nextConfig;
