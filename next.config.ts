import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['picsum.photos', 'liniaksara.com', 'liniaksara.id', 'liniaksara.org', 'images.unsplash.com'],
  },
};

export default nextConfig;
